spring:
  datasource:
    url: jdbc:mysql://localhost:3306/portfolio?useTimezone=true&serverTimezone=UTC
    username: root
    password:
    
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: false
    properties:
      hibernate:
        format_sql: false
        
jwt:
  secret: meiamesaetrescadeirashttcs
  expiration: 300000